{
  "name": "pingaspongas",
  "description": "A reimagined classic.",
  "version": "0.0.1",
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf ./build",
    "prebuild": "npm run clean && bash -c \"mkdir ./build\" && bash -c \"cp -r -t ./build ./src/index.html ./src/css/styles.css\"",
    "build": "npm run prebuild && node ./minify.js"
  },
  "dependencies": {
    "uglify-js": "^3.1.5"
  }
}
